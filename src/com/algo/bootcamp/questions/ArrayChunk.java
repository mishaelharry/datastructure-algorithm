package com.algo.bootcamp.questions;

import java.util.List;
import java.util.Stack;

public class ArrayChunk {

    public int[] chunk(int[] arr, int chunk){
        int[] chucked = new int[9];
        /*for (int i = 0; i < arr.length; i++){
            if (chucked.length > 0){
                //int last = chucked[chucked.length - 1];
            } else {
                chucked = arr[i];
            }
        }*/
        return chucked;
    }
}
